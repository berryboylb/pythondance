string = "The quick brown fox jumps over the lazy dog";
def count(str):
    split = str.split();
    # split the string and put it in an array
    stringLength = len(split)
    # get the length of the array
    print(stringLength)
    # print it out to the console
count(string)